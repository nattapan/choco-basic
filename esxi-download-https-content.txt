Firewall Rules:
By default, outbound HTTP/HTTPS connections might be restricted by the ESXi host firewall. If you encounter issues, you may need to enable the httpClient ruleset using 
esxcli network firewall ruleset set --enabled true --ruleset-id=httpClient
